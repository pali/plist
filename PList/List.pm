package PList::List;

use strict;
use warnings;

sub append($$) {
	die;
}

sub eof($) {
	die;
}

sub reset($) {
	die;
}

sub offset($) {
	die;
}

sub skipnext($) {
	die;
}

sub readnext($) {
	die;
}

1;
