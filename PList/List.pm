package PList::List;

use strict;
use warnings;

sub append($$) {
	die;
}

sub eof($) {
	die;
}

sub reset($) {
	die;
}

sub readnext($) {
	die;
}

sub count($) {
	die;
}

sub readnum($) {
	die;
}

# NEW:
# append($$)
# readat($$) # + duplicate fd + email::from_fd ( not shared )
# readnext($)
# eof($)
# reset($)

1;
