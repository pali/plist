package PList::List;

use strict;
use warnings;

sub append($$) {
	die;
}

sub readat($$) {
	die;
}

sub readnext($) {
	die;
}

sub offset($) {
	die;
}

sub eof($) {
	die;
}

sub reset($) {
	die;
}

1;
