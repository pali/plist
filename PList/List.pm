package PList::List;

use strict;
use warnings;

sub append($$) {
	die;
}

sub eof($) {
	die;
}

sub reset($) {
	die;
}

sub readnext($) {
	die;
}

sub count($) {
	die;
}

sub readnum($) {
	die;
}

1;
