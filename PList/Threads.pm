package PList::Threads;

use strict;
use warnings;

sub save($) {
	die;
}

sub add_email($$$$$) {
	die;
}

sub del_email($$) {
	die;
}

sub emails($) {
	die;
}

sub roots($) {
	die;
}

sub up($$) {
	die;
}

sub down($$) {
	die;
}

sub list($$) {
	die;
}

sub num($$) {
	die;
}

sub implicit($$) {
	die;
}

1;
